# P-101 temperature monitor

This synthetic P-101 utility combines temperature and vibration alarm counts for the operator summary. Run `pytest -q` to verify the aggregation rule.

The fixture deliberately contains one arithmetic defect for the governed coding-agent demonstration. Tests and CSV evidence are immutable during agent execution.
